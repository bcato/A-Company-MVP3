 
<div class="navbar-wrapper">
  <div class="container">
    <div class="navbar navbar-inverse">
      <div class="navbar-inner">
     
          <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
     
          <!-- Be sure to leave the brand out there if you want it shown -->
          <%= link_to "A Company", root_path, class: "brand" %>
          <%= form_tag instruments_path, :method => :get do %>

          <!-- Everything you want hidden at 940px or less, place within here -->
          <div class="nav-collapse collapse">
          	<ul class="nav">
              <li><%= link_to "Instruments", instruments_path %></li>
          </ul>
          <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li><%= link_to "Add +", new_instrument_path %></li>
                <li><%= link_to current_user.name, user_path(current_user) %></li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Sign Up", new_user_registration_path %></li>
              <li><%= link_to "Log In", new_user_session_path %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  How it All Works <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Overall", info_path %></li>
                  <li><%= link_to "For Renters", rent_info_path %></li>
                  <li><%= link_to "For Lenders", lend_info_path %></li>
                </ul>
              </li>
            <% end %>
          </ul>
          </div>
     
      </div>
    </div>
  </div>
</div>

<%#<!--<li><form class="nav pull-left">    
              <%= text_field_tag :search, params[:search], {placeholder: "Let's play something!", class: 'input-medium search-query'} %>
            
            </form>
          <% end %> <%#
        </li> %>

<div class="navbar-wrapper">
    <div class="navbar">
      
          <!-- Be sure to leave the brand out there if you want it shown -->
          <%= link_to(image_tag('Logos/andante-logo.png', :class => "brand"), root_path) %>
          <%= form_tag instruments_path, :method => :get do %>

          <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li><%= link_to "Add +", new_instrument_path %></li>
                <li><%= link_to current_user.first_name, user_path(current_user) %></li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Sign Up", new_user_registration_path %></li>
              <li><%= link_to "Log In", new_user_session_path %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  How it All Works <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Overall", info_path %></li>
                  <li><%= link_to "For Renters", rent_info_path %></li>
                  <li><%= link_to "For Lenders", lend_info_path %></li>
                </ul>
              </li>
            <% end %>
          </ul>

          <!-- Everything you want hidden at 940px or less, place within here -->
            
      
    </div>
</div>


          <% end %>
